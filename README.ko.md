# MuMiClick

<img src="src/MuMiClick/Assets/MuMiClick-logo.png" alt="MuMiClick 로고" width="96">

[English](README.md) · **한국어**

<img width="1035" height="979" alt="2026-07-28 15 47 48" src="https://github.com/user-attachments/assets/9d306f92-0b21-4a53-bde1-6660151771b1" />

MuMiClick은 Windows 10/11 x64에서 사용하는 마우스·키보드 매크로 녹화 프로그램입니다. 복잡한 스크립트 없이 쉽게 녹화하고 반복 재생할 수 있도록 만들었으며, 스캔 코드 기반 키보드 입력, 타임스탬프 기반 재생, 독립적인 긴급 중지 처리를 제공합니다.

모든 매크로 데이터는 컴퓨터에만 저장됩니다. 광고, 계정, 로그인, 텔레메트리, 외부 전송 기능이 없습니다.

## 다운로드

- [최신 Windows 포터블 릴리스](https://github.com/AppStudioLB/MuMiClick/releases/latest)
- 요구 사항: Windows 10 또는 Windows 11 x64
- 포터블 단일 파일에는 .NET 런타임이 포함되어 있습니다.

## 빠른 사용법

1. `MuMiClick.exe`를 실행합니다.
2. **녹화** 또는 `F8`을 누른 뒤 기록할 마우스·키보드 작업을 수행합니다.
3. `F8`을 다시 누르거나 **정지**를 누릅니다.
4. 반복 횟수, 재생 속도, 반복 간격을 정하고 **재생** 또는 `F9`를 누릅니다.
5. `F11`은 일시정지·재개, `F7`은 긴급 중지입니다.
6. **저장**으로 로컬 `.mumacro` JSON 파일을 만듭니다. 마지막으로 저장하거나 불러온 매크로는 다음 실행 때 자동 복원됩니다.

우측 상단의 **쉬운 모드**를 켜면 녹화, 정지, 재생, 반복 횟수, 무한 반복만 표시됩니다.

## 설정과 언어

우측 상단 **설정**에서 모든 전역 단축키와 표시 언어를 변경할 수 있습니다.

- `자동 (Windows)`: Windows 표시 언어가 한국어면 한국어, 그 외에는 영어
- `한국어`: 항상 한국어
- `English`: 항상 영어
- **다크 모드**: 프로그램 화면과 제목 표시줄을 어두운 테마로 전환

기본 전역 단축키는 다음과 같습니다.

| 기능 | 기본값 |
| --- | --- |
| 녹화 시작 / 종료 | `F8` |
| 재생 시작 | `F9` |
| 일시정지 / 재개 | `F11` |
| 긴급 중지 | `F7` |

단일 키와 `Ctrl+Alt+F8` 같은 조합을 지원합니다. 각 기능에는 서로 다른 단축키가 필요하며, 다른 프로그램이 사용 중인 단축키는 등록되지 않을 수 있습니다.

## 이벤트 목록

연속된 마우스 이동은 한 줄로 묶어 표시합니다. 각 그룹의 화살표 또는 **이동 펼치기** 버튼으로 원본 이동 이벤트를 확인할 수 있습니다.

- Shift 클릭으로 연속 범위를 선택합니다.
- Ctrl 클릭으로 개별 행을 선택에 추가하거나 제외합니다.
- 접힌 이동 그룹을 삭제하면 그룹에 포함된 모든 이동 이벤트가 삭제됩니다.
- 키보드 Delete 키도 지원합니다.

그룹 표시는 목록을 보기 쉽게 만드는 기능일 뿐이며, 저장 데이터와 재생 타이밍에는 원래 이동 이벤트가 그대로 유지됩니다.

## 액션별 편집

녹화된 마우스 또는 키보드 행 하나를 선택하고 **액션 편집**을 누르거나, 해당 행을 더블클릭하거나 `Enter`를 누릅니다.

- 마우스 이동·클릭·휠·키보드 키 누름 사이에서 액션 종류를 바꿀 수 있습니다.
- 마우스 X/Y 좌표, 버튼, 휠 이동량, 키, 누름 시간을 수정할 수 있습니다.
- 클릭과 키 입력은 항상 균형 잡힌 Down/Up 쌍으로 저장되어 편집 후 버튼이나 키가 눌린 상태로 남지 않습니다.
- 드래그 중 마우스 이동이나 단축키 내부 키처럼 Down/Up 사이에 있던 액션은 원래 위치를 유지합니다.
- 묶인 마우스 이동은 먼저 펼친 뒤 이동 하나를 선택해 편집합니다.

Down 또는 Up 행 중 하나만 선택해도 연결된 쌍을 자동으로 찾아 함께 편집합니다. 랜덤 갈래는 기존 전용 갈래 편집기를 사용합니다. 클립보드·저장 창 대기·창 텍스트 대기 같은 제어 이벤트는 삭제한 뒤 각각의 상단 버튼으로 다시 넣을 수 있습니다.

## 변수와 클립보드 이벤트

이벤트 목록 위의 **변수**에서 재사용할 이름/값을 등록합니다. 선택 항목인 **그룹** 열에 같은 이름을 입력하면 해당 변수들이 하나의 무작위 후보 묶음이 됩니다.

값이 필요한 순서를 선택하고 **클립보드 넣기**를 누르면 두 가지 방식을 고를 수 있습니다.

- **고정 변수 하나 사용**은 선택한 변수의 값을 항상 넣습니다.
- **변수 그룹에서 무작위 선택**은 이벤트를 재생할 때마다 그룹 구성원 하나를 서로 독립적으로 균등하게 고릅니다.

클립보드 이벤트 바로 다음에 `Ctrl+V` 입력을 녹화하거나 배치하면 선택된 값을 활성 입력란에 붙여넣습니다.

변수와 클립보드 이벤트는 `.mumacro` 파일 안에 함께 저장됩니다. 다른 프로그램이 클립보드를 잠근 경우 짧게 재시도하며, 실제 변수 값은 로그에 남기지 않습니다.

## 랜덤 행동 다발

**랜덤 갈래**는 다른 매크로 파일을 불러오는 기능이 아니라, 현재 녹화 목록에 있는 행동을 여러 다발로 묶는 기능입니다.

1. **랜덤 갈래**를 열고 왼쪽에서 마우스 이동·클릭·키 입력을 선택합니다. Shift 클릭으로 범위를 고를 수 있습니다.
2. **선택 → 갈래 추가**를 눌러 첫 번째 행동 다발을 만듭니다.
3. 현재 이벤트에서 다른 범위를 선택해 두 번째 이상의 다발을 추가합니다.
4. 적용하면 원본 행동들이 하나의 랜덤 갈래 행으로 바뀝니다.

재생이 이 행에 도달할 때마다 다발 하나만 균등하게 무작위 선택됩니다. 선택된 다발 안에서는 원래 이벤트 순서와 상대 시간 간격을 유지합니다. 랜덤 갈래 행을 선택하고 **랜덤 갈래**를 다시 누르면 이름과 대안을 편집할 수 있습니다.

## 저장 창 안정화

브라우저나 파일 저장 창이 늦게 열리는 작업에서는 고급 모드의 **저장 창 자동 대기**를 켜세요. 녹화 시 대기 이벤트가 추가되고, 재생 중에는 저장 창과 입력 컨트롤이 준비될 때까지 기다립니다. 제한 시간은 1~60초이며 기본값은 15초입니다.

기존 매크로에는 이벤트 목록의 **저장 창 대기** 버튼으로 같은 동작을 삽입할 수 있습니다.

## 크롬 또는 프로그램 창의 텍스트 기다리기

메시지·레이블·버튼·상태 문구가 나타난 뒤에만 다음 행동을 실행해야 한다면 이벤트 목록 위의 **텍스트 기다리기**를 사용합니다.

1. 재생을 멈춰 둘 이벤트 위치를 선택하고 **텍스트 기다리기**를 누릅니다.
2. 감시할 크롬 또는 프로그램 창을 선택합니다.
3. 감지할 문구를 입력하고 **포함** 또는 **전체 일치**를 고릅니다.
4. 제한시간을 1~3600초로 입력하거나, 무제한 대기는 `0`을 입력합니다.

재생 중 350ms 간격으로 해당 창의 Windows 접근성 트리를 확인합니다. 문구가 나타나면 기다린 시간을 재생 타임라인에 보정한 뒤 다음 행동을 실행하므로 후속 입력이 한꺼번에 몰리지 않습니다. 대기 중에도 일시정지·재개와 긴급 중지가 작동합니다. 페이지 이동으로 크롬 창 제목이 바뀌어도 같은 프로세스와 창 클래스가 하나로 명확하면 안전하게 다시 찾습니다.

이 기능은 OCR이 아니라 접근성 텍스트 감지입니다. 캔버스·이미지에만 그려졌거나 접근성 API에 숨겨진 텍스트는 감지할 수 없습니다. Chromium의 Windows 접근성 트리 노출 방식은 [Chromium 접근성 개요](https://chromium.googlesource.com/chromium/src/+/main/docs/accessibility/overview.md)를 참고하세요.

## 빠른 마우스 이동

**클릭 직전 순간 이동**을 켜면 재생 중간의 마우스 이동을 생략하고 클릭, 휠, 드래그 종료 직전의 정확한 좌표로 이동합니다. 드래그 인식 안정성을 위해 기본 30ms의 최소 지연을 사용합니다.

## 좌표 모드

- **화면 절대 좌표**는 Windows 가상 데스크톱을 기준으로 재생하며 음수 좌표 멀티 모니터를 지원합니다.
- **대상 창 상대 좌표**는 재생 전 대상 창을 다시 찾아, 창을 옮겨도 같은 내부 위치를 클릭합니다. 대상 창이 없으면 재생하지 않습니다.

대상 프로그램을 나중에 열었다면 대상 창 선택 창의 **새로고침**을 누르세요.

## 입력 안정성과 안전

- `WH_MOUSE_LL`, `WH_KEYBOARD_LL`로 입력을 녹화합니다.
- 문자 변환이 아닌 스캔 코드 기반 `SendInput`으로 KeyDown과 KeyUp을 각각 전송합니다.
- 마우스와 키보드는 하나의 시간순 목록으로 관리합니다.
- 상대 Sleep 누적 대신 녹화 시작점 기준 타임스탬프로 재생합니다.
- 자체 주입 입력은 `dwExtraInfo`로 표시해 녹화에서 제외합니다.
- 눌린 키와 마우스 버튼을 추적해 중지, 오류, 완료 시 강제로 해제합니다.
- **실제 입력 시 중지**는 기본으로 켜져 있으며 사용자가 실제 마우스나 키보드를 조작하면 즉시 재생을 중지합니다.
- 긴급 중지는 별도 취소 경로를 사용하며 반복 대기 중에도 동작합니다.
- Per-monitor-v2 DPI와 `MOUSEEVENTF_VIRTUALDESK`로 125%/150% 배율과 멀티 모니터를 지원합니다.

Windows UIPI 때문에 일반 권한 프로그램은 관리자 권한 창에 입력하지 못할 수 있습니다. 이런 대상에는 **관리자 재실행**을 사용하고 UAC를 승인하세요.

## 빌드

```powershell
dotnet build MuMiClick.sln -c Release
dotnet publish src\MuMiClick\MuMiClick.csproj -c Release -p:PublishProfile=Portable-win-x64 -o .\release\MuMiClick-win-x64
```

## 자동 테스트

```powershell
dotnet run --project tests\MuMiClick.SmokeTests\MuMiClick.SmokeTests.csproj -c Release
```

안전 기본값, 언어 선택, 단일 키 단축키 파싱, JSON 왕복, KeyDown/KeyUp 보존, 드래그·단축키 사이의 입력 쌍 탐색, 마우스 클릭→키 입력 변환, 스캔 코드 Down/Up 생성, 저장 창 대기 이벤트, 창 텍스트 트리거, 포함·전체 일치, 실제 Win32 접근성 트리 텍스트 감지, Chrome 접근성 연결, 타임스탬프 순서, 마우스 이동 표시 그룹, 변수 그룹, 고정·무작위 클립보드 이벤트, 랜덤 행동 다발 저장, 갈래 선택, 편집 창 생성을 검사합니다.

## 알려진 제한사항

- 저수준 훅과 `SendInput`은 Windows에서만 동작합니다.
- UIPI 때문에 MuMiClick은 대상과 같거나 높은 권한으로 실행해야 합니다.
- 보안 데스크톱, UAC 자격 증명 화면, 일부 안티치트 프로그램, 합성 입력을 거부하는 프로그램은 자동화할 수 없습니다.
- 창 상대 좌표는 저장된 프로세스, 제목, 창 클래스로 대상을 찾으므로 제목이 자주 바뀌는 프로그램은 다시 선택해야 할 수 있습니다.
- 창 텍스트 트리거는 대상 프로그램이 Windows 접근성 트리에 공개한 텍스트만 감지합니다. 캔버스·이미지 전용·보호된 콘텐츠에는 다른 트리거가 필요합니다.
- 실제 멀티 모니터와 IME 조합은 사용할 컴퓨터에서 수동 확인이 필요합니다.

## 개인정보

녹화는 사용자가 버튼이나 단축키로 명시적으로 시작한 경우에만 수행됩니다. 데이터는 로컬에 저장되며 로그에는 실제 입력 문자열을 남기지 않습니다.

## 라이선스

[MuMiClick Community License](LICENSE)를 적용합니다. 사용, 복제, 수정, 재배포는 가능하지만 수정·재배포한 버전을 공식 MuMiClick 프로젝트인 것처럼 소개하거나, 혼동을 유발하는 방식으로 MuMiClick 이름·로고·브랜딩을 사용하면 안 됩니다. 불법적·기만적·개인정보 침해·유해·악의적 사용은 금지됩니다.
